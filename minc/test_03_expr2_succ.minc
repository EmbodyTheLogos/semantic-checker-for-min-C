num main()
{
    num x;
    num y;
    bool z;
    x = 0;
    x = x + 1;
    y = 1.0 * 2.1 + 3.5 / (4.1 - 5.3);
    z = not true and 1 < 3 and y >= 4.2 or true == (x < 2);
    print z;
    return y;
}
