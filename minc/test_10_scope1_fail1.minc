num main(num a, num b)
{
    bool a;         // Since a is already defined as parameter of main() function, a cannot be defined as a new variable here.
    num lb;
    {
        bool a;     // However, redefining a in a new composite_stmt is allowed.
        bool b;
        bool ga;
        ga = true;
        a = true;
        b = true;
    }
    a = true;
    b = 2.0;
    ga = 3;
    gb = 4.0;
    gc = true;
    {
        bool la;
        la = true;
    }
    return 0;
}
